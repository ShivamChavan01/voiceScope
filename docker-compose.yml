version: "3.8"

services:
  voicescope:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      - DATA_DIR=/app/data
      - DATABASE_URL=postgresql://voicescope:voicescope@postgres:5432/voicescope
    volumes:
      - voicescope_data:/app/data
    depends_on:
      postgres:
        condition: service_healthy
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "python", "-c", "import httpx; httpx.get('http://localhost:8000/api/v1/health').raise_for_status()"]
      interval: 30s
      timeout: 10s
      retries: 3

  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: voicescope
      POSTGRES_PASSWORD: voicescope
      POSTGRES_DB: voicescope
    volumes:
      - pg_data:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U voicescope"]
      interval: 5s
      timeout: 5s
      retries: 5

volumes:
  voicescope_data:
  pg_data:
